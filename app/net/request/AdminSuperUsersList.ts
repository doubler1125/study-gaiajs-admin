/**
 * generated by template generator. don't modify it.
 *
 */

import { Context } from 'egg';
import GaiaRequest from 'study-gaiajs/app/lib/request';


/**
 * path: /admin/super/admin_users
 *
 * 查看管理员
 */
export default class AdminSuperUsersListRequest extends GaiaRequest {
  static accessorName = 'AdminSuperUsersList';
  static path = '/admin/super/admin_users';
  static method = 'GET';

  /**
   *
   * @type number
   * @memberof AdminSuperUsersList
   */
  skip?: number;

  /**
   *
   * @type number
   * @memberof AdminSuperUsersList
   */
  limit?: number;

  // tslint:disable-next-line: max-func-body-length
  constructor(params: any, ctx: Context) {
    super(params, ctx);
    const data: {
      [K: string]: any;
    } = {};

    
    if (ctx.query.hasOwnProperty('skip')) {
      data.skip = ctx.query.skip;
    }
    
    if (ctx.query.hasOwnProperty('limit')) {
      data.limit = ctx.query.limit;
    }
    

    const rules = {
      skip: {
        type: 'number', required: false, },
      limit: {
        type: 'number', required: false, },

    };

    try {
      ctx.validate(rules, data);
    } catch (err) {
      ctx.service.error.throwRequestValidateError(err);
    }

    Object.defineProperties(this, {
      skip: { value: data.skip },
      limit: { value: data.limit },
    });

    Object.defineProperty(this, 'toJSON', {
      value: () => {
        return data;
      },
    });
  }
}
